{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open in Chrome",
            "request": "launch",
            "type": "chrome",
            "file": "${workspaceFolder}/Index.html"
        }
    ]
}
